<%= form_for(@medication) do |f| %>
  <% if @medication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medication.errors.count, "error") %> prohibited this medication from being saved:</h2>

      <ul>
      <% @medication.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :generic_name %><br />
    <%= f.text_field :generic_name %>
  </div>
  <div class="field">
    <%= f.label :secondary_effects %><br />
    <%= f.text_area :secondary_effects %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
